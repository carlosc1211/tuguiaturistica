grecaptcha.ready(function() {
    grecaptcha.execute('6LcJ9HwUAAAAAC6PilyCGI1RwKftroxEJSwbYDg9', {action: 'action_name'})
    .then(function(token) {
    // Verify the token on the server.
    });
    });